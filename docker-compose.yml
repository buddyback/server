services:
    buddy-back-server:
        build: .
        ports:
            - "8000:8000"
        container_name: buddy-back-server
        restart: unless-stopped
        command: >
            sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8001"
        pull_policy: build